apiVersion: v1
kind: Service
metadata:
  name: task-queue-worker
  namespace: task-queue-worker
  labels:
    app: task-queue-worker
spec:
  selector:
    app: task-queue-worker
  ports:
  - port: 8000
    targetPort: 8000
    name: http